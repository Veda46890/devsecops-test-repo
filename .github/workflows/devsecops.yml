name: DevSecOps Security Pipeline

on:

  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:

  # =========================================================
  # SAST - SEMGREP
  # =========================================================

  sast-semgrep:

    name: SAST Security Scan

    runs-on: ubuntu-latest

    steps:

      # =====================================================
      # CHECKOUT REPOSITORY
      # =====================================================

      - name: Checkout Repository
        uses: actions/checkout@v4

      # =====================================================
      # INSTALL SEMGREP
      # =====================================================

      - name: Install Semgrep
        run: |

          python3 -m pip install semgrep

      # =====================================================
      # RUN SEMGREP SCAN
      # =====================================================

      - name: Run Semgrep Scan
        run: |

          semgrep scan \
            --config=auto \
            --json \
            --output semgrep-report.json

      # =====================================================
      # INSTALL JQ
      # =====================================================

      - name: Install jq
        run: |

          sudo apt-get update

          sudo apt-get install -y jq

      # =====================================================
      # SHOW SEMGREP RESULTS
      # =====================================================

      - name: Show Semgrep Results
        run: |

          echo "===================================="

          echo "SEMGREP RESULTS"

          echo "===================================="

          cat semgrep-report.json | jq '.results | length'

      # =====================================================
      # ALWAYS UPLOAD ARTIFACT
      # IMPORTANT
      # =====================================================

      - name: Upload SAST Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-sast-report
          path: semgrep-report.json

      # =====================================================
      # SECURITY GATE
      # FAIL PIPELINE FOR HIGH/CRITICAL
      # =====================================================

      - name: SAST Security Gate
        run: |

          CRITICAL=$(cat semgrep-report.json | jq '[.results[] | select(.extra.severity=="ERROR")] | length')

          HIGH=$(cat semgrep-report.json | jq '[.results[] | select(.extra.severity=="WARNING")] | length')

          echo "===================================="

          echo "SEMGRP SECURITY GATE"

          echo "===================================="

          echo "Critical Findings: $CRITICAL"

          echo "High Findings: $HIGH"

          echo "===================================="

          if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 0 ]; then

            echo "SAST SECURITY GATE FAILED"

            exit 1

          fi

  # =========================================================
  # SCA - TRIVY
  # =========================================================

  sca-trivy:

    name: SCA Security Scan

    runs-on: ubuntu-latest

    steps:

      # =====================================================
      # CHECKOUT REPOSITORY
      # =====================================================

      - name: Checkout Repository
        uses: actions/checkout@v4

      # =====================================================
      # RUN TRIVY FILESYSTEM SCAN
      # =====================================================

      - name: Run Trivy Filesystem Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          format: json
          output: trivy-report.json

      # =====================================================
      # INSTALL JQ
      # =====================================================

      - name: Install jq
        run: |

          sudo apt-get update

          sudo apt-get install -y jq

      # =====================================================
      # SHOW TRIVY RESULTS
      # =====================================================

      - name: Show Trivy Results
        run: |

          echo "===================================="

          echo "TRIVY RESULTS"

          echo "===================================="

          cat trivy-report.json | jq '.Results | length'

      # =====================================================
      # ALWAYS UPLOAD ARTIFACT
      # IMPORTANT
      # =====================================================

      - name: Upload SCA Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-sca-report
          path: trivy-report.json

      # =====================================================
      # SECURITY GATE
      # FAIL PIPELINE FOR HIGH/CRITICAL
      # =====================================================

      - name: SCA Security Gate
        run: |

          CRITICAL=$(cat trivy-report.json | jq '[.Results[].Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length')

          HIGH=$(cat trivy-report.json | jq '[.Results[].Vulnerabilities[]? | select(.Severity=="HIGH")] | length')

          echo "===================================="

          echo "TRIVY SECURITY GATE"

          echo "===================================="

          echo "Critical Findings: $CRITICAL"

          echo "High Findings: $HIGH"

          echo "===================================="

          if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 0 ]; then

            echo "SCA SECURITY GATE FAILED"

            exit 1

          fi

  # =========================================================
  # SECURITY PIPELINE SUMMARY
  # =========================================================

  security-summary:

    name: Security Pipeline Summary

    runs-on: ubuntu-latest

    needs:
      - sast-semgrep
      - sca-trivy

    if: always()

    steps:

      - name: Security Summary
        run: |

          echo "======================================"

          echo "DEVSECOPS SECURITY PIPELINE COMPLETED"

          echo "======================================"

          echo "SAST : Semgrep Completed"

          echo "SCA  : Trivy Completed"

          echo "======================================"
